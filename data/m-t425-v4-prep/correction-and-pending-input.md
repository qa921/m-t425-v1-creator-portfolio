# M-T425-V4 — correction and pending-assets input

This is an intentionally unresolved handoff input, not a completed asset register.

## Required second pass after a first deploy
- Re-open each selected stable repository path rather than trusting a prior label or extension.
- Compare each displayed title, media type, sourceMediaId, permalink, and metric label against a fresh Instagram read.
- Replace a failed, stale, or mislabeled reference with a separately verified alternative. Do not substitute an uncertain asset.
- Redeploy and check the resulting deployment status/URL.

## Existing unresolved candidates for the eventual pending-assets handoff
1. `18142948204602129` Table Story 07 — prior state has no local stored file.
2. `17993741376042936` Drafts 13 — prior state has no local stored file.
3. `17902629885570895` QA fixture — explicitly disposable; never feature without new independent approval.
4. Reel container `18129047179753804` — attempted source video processing returned `ERROR`; no published media ID.
5. Reel container `18129047242753804` — alternate attempted source video processing returned `ERROR`; no published media ID.
6. Contact `hello@example.invalid` — placeholder, awaiting approved contact detail.
7. Collaboration `Northstar Studio` — unsupported placeholder claim, awaiting evidence or approval.

## Notes
The live source set now contains 22 parent media records: 20 labeled portfolio inputs, the M-T425-V4 carousel, and one older QA fixture. The M-T425-V4 carousel is live and has child IDs `18631049461043875` and `18127359880823623`. Its CDN URLs are source-only and must be re-fetched before use. No live reel was established because both attempted containers failed processing; preserve this as a pending exception rather than inventing a reel asset.
